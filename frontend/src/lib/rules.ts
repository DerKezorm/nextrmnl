/** Rules shared by the server and the UI. The server checks them again; this is about feedback while typing. */

/** Passwords for accounts and archives: nextrmnl holds the access to every machine, hence more than elsewhere. */
export const MIN_PASSWORD = 12
